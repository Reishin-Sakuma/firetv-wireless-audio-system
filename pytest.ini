[tool:pytest]
minversion = 6.0
addopts = -ra -q --strict-markers --strict-config
testpaths = tests
markers =
    unit: Unit tests
    integration: Integration tests
    bluetooth: Bluetooth related tests
    wifi: WiFi related tests
    audio: Audio processing tests
    slow: Slow running tests